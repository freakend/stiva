# Stiva is a fully client-side app — no backend, no API keys, no secrets.
# Everything below is just a placeholder reference. Add real keys here
# only if you later introduce external services (analytics, tiles, etc.).